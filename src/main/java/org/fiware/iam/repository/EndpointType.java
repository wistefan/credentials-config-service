package org.fiware.iam.repository;

/**
 * Type of an {@link EndpointEntry}, either being a trusted issuers list or a trusted participants list
 */
public enum EndpointType {

	TRUSTED_ISSUERS,
	TRUSTED_PARTICIPANTS
}
